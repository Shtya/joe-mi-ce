users:
			✅ add mobile in create ( back and front )
			✅ add filter in list users ( back and front ) 

Products: 
			✅ accept edit all data in create ( back and front )
			✅ filter by ( category / brand / name ) ( back and front )


Stock: 
			✅ get stock for project -> Filter by ( branch /  category / brand / product / create date )

Branches : 
			✅ filters ( branch / region / city / chain  ) ( back and front )

Journeys: 
			✅ filter ( users / branch / region / city ) ( back and front )
			✅ get by project
			✅ get all unplaned  filter ( status / users / branch / region / city / from date / to date ) 


Sales :
			✅ filter ( username / brand / category / chain / region / city / branch / promoters / from date / to date ) 
			✅ missing on the relations of the sells 

Feedback:
			✅ Crud ( Back & Front )

Audit:
			✅ filter ( username / brand / category / chain / region / city / branch / promoters / from date / to date ) ( Back & Front )

Survey :
			✅ get questions on servery 


✅ export 
✅ check on add to supervisor
✅ stats 
✅ notifications to the supervisor 


